import { View } from 'react-native';

/**
 * Placeholder — will show suggested add-ons from the same restaurant.
 * Currently renders nothing until the suggestion API is wired.
 */
export function CartSuggestionsFromStore() {
  return <View />;
}
