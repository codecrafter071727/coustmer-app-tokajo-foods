import { PublicShareTrackingScreen } from '@/components/order/PublicShareTrackingScreen';

/** Public family track — no login (GET /tracking/share/:shareToken). */
export default function PublicShareTrackPage() {
  return <PublicShareTrackingScreen />;
}
